---
import { getAllVideos, getAllCategories, generateSlug } from '../utils/videoUtils';
import { getCollection } from 'astro:content';
import { PARTNERS } from '../config/site';

const currentPath = Astro.url.pathname;

const allVideos = await getAllVideos();
const totalCount = allVideos.length;
const allCategories = await getAllCategories();

const categoryCounts = new Map<string, number>();
for (const cat of allCategories) {
  categoryCounts.set(cat, allVideos.filter((v: any) => v.data.category === cat).length);
}

const isHome = currentPath === '/' || currentPath.startsWith('/page/');
const isAboutActive = currentPath === '/about';
const isResourcesActive = currentPath.startsWith('/resources');
const isLogoAnimActive = currentPath.startsWith('/logo-animation');

const allResources = await getCollection('resources');
const resourceCount = allResources.length;

---

<!-- Mobile overlay -->
<div class="sidebar-overlay" id="sidebar-overlay"></div>

<aside class="sidebar" id="sidebar">
  <div class="sidebar-inner">

    <!-- Close button (mobile only) -->
    <button class="sidebar-close" id="sidebar-close" aria-label="Close menu">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round">
        <path d="M18 6L6 18M6 6l12 12"/>
      </svg>
    </button>

    <!-- Logo -->
    <a href="/" class="sidebar-logo">
      <span>Showreel</span>
      <span>.Design</span>
    </a>

    <!-- Search nav item -->
    <button class="sidebar-item sidebar-item-icon" id="sidebar-search-btn">
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" class="nav-icon" xmlns="http://www.w3.org/2000/svg">
        <path fill-rule="evenodd" clip-rule="evenodd" d="M1 0.99999H6.09618V2.33332H2.33333V6.09617H1V0.99999ZM13.6667 2.33332H9.90382V0.99999H15V6.09617H13.6667V2.33332ZM15 9.90381V15H9.90382V13.6667H13.6667V9.90381H15ZM2.33333 13.6667V9.90381H1V15H6.09618V13.6667H2.33333Z" fill="currentColor"/>
        <path fill-rule="evenodd" clip-rule="evenodd" d="M4 7.66667C4 5.64162 5.64162 4 7.66667 4C9.69171 4 11.3333 5.64162 11.3333 7.66667C11.3333 8.43833 11.095 9.15432 10.6878 9.74502L11.8047 10.8619L10.8619 11.8047L9.74502 10.6878C9.15432 11.095 8.43833 11.3333 7.66667 11.3333C5.64162 11.3333 4 9.69171 4 7.66667ZM7.66667 5.33333C6.378 5.33333 5.33333 6.378 5.33333 7.66667C5.33333 8.95533 6.378 10 7.66667 10C8.95533 10 10 8.95533 10 7.66667C10 6.378 8.95533 5.33333 7.66667 5.33333Z" fill="currentColor"/>
      </svg>
      <span class="nav-label">Search</span>
    </button>
    <div class="sidebar-search-divider"></div>

    <!-- Main nav: Category + Resource sections -->
    <div class="sidebar-sections">

      <!-- Category section -->
      <div class="sidebar-section">
        <p class="sidebar-section-label">Showreels</p>
        <nav class="sidebar-nav">
          <a href="/" class={`sidebar-item sidebar-item-icon ${isHome ? 'active' : ''}`}>
            <img src="/uploads/more-03.svg" class="nav-icon" alt="" />
            <span class="nav-label">All</span>
            <span class="sidebar-badge badge-hover">{totalCount}</span>
          </a>
          {allCategories.map((cat) => {
            const slug = generateSlug(cat);
            const isActive = currentPath.includes(`/category/${slug}`);
            const iconMap: {[key: string]: string} = {
              'Motion Graphics':    'ease-curve-control-points',
              '3D / CGI':           '3d-view',
              'Studio / Agency':    'global',
              'Branding & Identity':'pen-tool-03',
              'UI/UX Motion':       'web-design-02',
            };
            const icon = iconMap[cat] ?? 'more-03';
            return (
              <a href={`/category/${slug}`} class={`sidebar-item sidebar-item-icon ${isActive ? 'active' : ''}`}>
                <img src={`/uploads/${icon}.svg`} class="nav-icon" alt="" />
                <span class="nav-label">{cat}</span>
                <span class="sidebar-badge badge-hover">{categoryCounts.get(cat) ?? 0}</span>
              </a>
            );
          })}
        </nav>
      </div>
      <div class="sidebar-search-divider"></div>
      {import.meta.env.CONTEXT !== 'production' && (
      <div class="sidebar-section">
        <nav class="sidebar-nav">
          <a href="/resources" class={`sidebar-item sidebar-item-icon ${isResourcesActive ? 'active' : ''}`}>
            <img src="/uploads/folder-03.svg" class="nav-icon" alt="" />
            <span class="nav-label">Resources</span>
            <span class="sidebar-badge badge-hover">{resourceCount}</span>
          </a>
          <a href="/logo-animation" class={`sidebar-item sidebar-item-icon ${isLogoAnimActive ? 'active' : ''}`}>
            <img src="/uploads/bookmark-01.svg" class="nav-icon" alt="" />
            <span class="nav-label">Logo Animation</span>
          </a>
        </nav>
      </div>
      )}

    </div>

    <!-- Spacer -->
    <div class="sidebar-spacer"></div>

    <!-- Footer nav -->
    <div class="sidebar-footer">
      <a href="/about" class={`sidebar-item ${isAboutActive ? 'active' : ''}`}>About Us</a>

      <!-- Follow Us on X -->
      <a href="https://x.com/showreeldesign" target="_blank" rel="noopener noreferrer" class="sidebar-item">Follow Us on X</a>
<div class="sidebar-search-divider"></div>
      <!-- Partner banner slider -->
      <div class="sidebar-partner">
        <span class="sidebar-partner-label">Partner</span>
        <div class="partner-slider" id="partner-slider">
          <div class="partner-track" id="partner-track">
            {PARTNERS.map((p) => (
              <a href={p.href} target="_blank" rel="noopener noreferrer" class="partner-slide">
                <img src={p.img} alt={p.name} loading="lazy" />
                <span class="slide-tooltip">{p.name}</span>
              </a>
            ))}
          </div>

          <button class="slider-btn slider-prev" id="slider-prev" aria-label="Previous">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M15 18l-6-6 6-6"/>
            </svg>
          </button>
          <button class="slider-btn slider-next" id="slider-next" aria-label="Next">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 18l6-6-6-6"/>
            </svg>
          </button>

          <div class="slider-dots" id="slider-dots">
            {PARTNERS.map((_, i) => (
              <button class={`slider-dot ${i === 0 ? 'active' : ''}`} data-index={i}></button>
            ))}
          </div>
        </div>
      </div>

      <!-- Copyright -->
      <div class="sidebar-copyright">
        <div class="sidebar-copyright-divider"></div>
        <p class="sidebar-copyright-text">All showreel © of their respective owners</p>
      </div>

    </div>

  </div>
</aside>

<!-- Search modal (global, outside sidebar) -->
<div class="sm-overlay" id="sm-overlay">
  <div class="sm-modal" id="sm-modal">
    <div class="sm-input-row">
      <img src="/uploads/MagnifyingGlass.svg" class="sm-search-icon" alt="" />
      <input id="sm-input" type="text" placeholder="Search showreels..." autocomplete="off" />
      <kbd class="sm-esc-key">Esc</kbd>
    </div>
    <div class="sm-divider"></div>
    <div class="sm-results" id="sm-results">
      <p class="sm-hint">Type to search...</p>
    </div>
  </div>
</div>

<script is:inline>
// @ts-nocheck
// Search modal
(function() {
  var btn     = document.getElementById('sidebar-search-btn');
  var overlay = document.getElementById('sm-overlay');
  var modal   = document.getElementById('sm-modal');
  var input   = document.getElementById('sm-input');
  var results = document.getElementById('sm-results');

  var allVideos = null;
  var debounce  = null;

  function open() {
    overlay.classList.add('open');
    if (window.lenis) window.lenis.stop();
    input.focus();
    if (!allVideos) {
      fetch('/api/videos.json')
        .then(function(r) { return r.json(); })
        .then(function(d) {
          allVideos = d;
          render(input.value);
        })
        .catch(function() {
          results.innerHTML = '<p class="sm-hint">Search unavailable.</p>';
        });
    }
  }

  function close() {
    overlay.classList.remove('open');
    if (window.lenis) window.lenis.start();
    input.value = '';
    results.innerHTML = '<p class="sm-hint">Type to search...</p>';
  }

  function initials(name) {
    return (name || '').split(' ').slice(0, 2).map(function(w) { return w[0] || ''; }).join('').toUpperCase();
  }

  function renderItem(v) {
    var avatar = v.authorAvatar
      ? '<img src="' + v.authorAvatar + '" class="sm-item-avatar" alt="" loading="lazy" />'
      : '<div class="sm-item-initials">' + initials(v.author) + '</div>';
    return '<a href="/videos/' + v.slug + '" class="sm-item">' +
      avatar +
      '<span class="sm-item-title">' + v.title + '</span>' +
      '<span class="sm-item-cat">' + (v.category || '') + '</span>' +
    '</a>';
  }

  function render(q) {
    if (!allVideos) return;
    if (!q.trim()) {
      results.innerHTML = '<p class="sm-hint">Type to search...</p>';
      return;
    }
    var term = q.toLowerCase();
    var matches = allVideos.filter(function(v) {
      return (v.title || '').toLowerCase().includes(term) ||
             (v.author || '').toLowerCase().includes(term) ||
             (v.description || '').toLowerCase().includes(term) ||
             (v.tags || []).some(function(t) { return t.toLowerCase().includes(term); });
    });
    if (matches.length === 0) {
      results.innerHTML = '<p class="sm-hint">No results for "' + q + '"</p>';
      return;
    }
    results.innerHTML = matches.map(renderItem).join('');
  }

  btn.addEventListener('click', open);

  overlay.addEventListener('click', function(e) {
    if (!modal.contains(e.target)) close();
  });

  // Block wheel events from reaching Lenis (listens at window level)
  modal.addEventListener('wheel', function(e) { e.stopPropagation(); }, { passive: true });

  input.addEventListener('input', function() {
    clearTimeout(debounce);
    debounce = setTimeout(function() { render(input.value); }, 180);
  });

  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') close();
    if ((e.key === 'k' || e.key === 'K') && (e.metaKey || e.ctrlKey)) {
      e.preventDefault();
      overlay.classList.contains('open') ? close() : open();
    }
  });
})();
</script>

<script is:inline>
// @ts-nocheck
// Partner slider
(function() {
  var slider = document.getElementById('partner-slider');
  var track  = document.getElementById('partner-track');
  var dots   = document.querySelectorAll('#slider-dots .slider-dot');
  var prevBtn = document.getElementById('slider-prev');
  var nextBtn = document.getElementById('slider-next');
  var total = dots.length;
  var current = 0;
  var autoTimer;

  function goTo(index) {
    current = (index + total) % total;
    if (track) track.style.transform = 'translateX(-' + (current * 100) + '%)';
    dots.forEach(function(d, i) { d.classList.toggle('active', i === current); });
  }

  function startAuto() { autoTimer = setInterval(function() { goTo(current + 1); }, 5000); }
  function stopAuto()  { clearInterval(autoTimer); }

  prevBtn && prevBtn.addEventListener('click', function(e) { e.preventDefault(); stopAuto(); goTo(current - 1); startAuto(); });
  nextBtn && nextBtn.addEventListener('click', function(e) { e.preventDefault(); stopAuto(); goTo(current + 1); startAuto(); });
  dots.forEach(function(dot) { dot.addEventListener('click', function() { stopAuto(); goTo(Number(dot.dataset.index)); startAuto(); }); });
  slider && slider.addEventListener('mouseenter', stopAuto);
  slider && slider.addEventListener('mouseleave', startAuto);

  startAuto();
})();
</script>

<style>
  /* ── Mobile overlay ── */
  .sidebar-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.3);
    z-index: 90;
  }
  .sidebar-overlay.open { display: block; }

  /* ── Close button (mobile only) ── */
  .sidebar-close { display: none; }

  /* ── Sidebar ── */
  .sidebar {
    position: fixed;
    left: 0;
    top: 0;
    height: 100vh;
    width: 209px;
    z-index: 100;
    transition: transform 0.3s ease;
  }

  .sidebar-inner {
    display: flex;
    flex-direction: column;
    height: 100%;
    padding: 12px;
    background: #fff;
    border-right: 1px solid #f0f0f0;
    overflow-y: auto;
  }

  /* ── Logo ── */
  .sidebar-logo {
    display: flex;
    flex-direction: column;
    font-size: 20px;
    font-weight: 700;
    line-height: 0.97;
    color: #000;
    text-decoration: none;
    margin-bottom: 32px;
    flex-shrink: 0;
    letter-spacing: -0.3px;
  }
  .sidebar-logo:hover { opacity: 0.7; }

  /* ── Search nav item divider ── */
  .sidebar-search-divider {
    width: 100%;
    height: 1px;
    background: #f0f0f0;
    margin-top: 8px;
    margin-bottom: 8px;
    flex-shrink: 0;
  }

  /* ── Sections ── */
  .sidebar-sections {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .sidebar-section {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .sidebar-section-label {
    font-size: 12px;
    color: #a4a4a4;
    line-height: 1.191;
    margin: 0;
    padding: 0 8px;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  /* ── Nav items ── */
  .sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .sidebar-item {
    font-size: 13px;
    color: #555;
    text-decoration: none;
    transition: color 0.15s, background 0.15s;
    line-height: 1.191;
    background: none;
    border: none;
    padding: 0 8px;
    cursor: pointer;
    text-align: left;
    width: 100%;
    font-family: 'Aileron', sans-serif;
    border-radius: 8px;
    height: 32px;
    display: flex;
    align-items: center;
  }

  .sidebar-item:hover {
    color: #222;
    background: rgba(90, 90, 90, 0.04);
  }

  .sidebar-item.active {
    color: #111;
    background: rgba(90, 90, 90, 0.06);
    box-shadow:
      0px -1px 0px 0px rgba(255,255,255,0.35),
      1px  0px 0px 0px rgba(255,255,255,0.15),
      -1px 0px 0px 0px rgba(255,255,255,0.15),
      0px  1px 0px 0px rgba(255,255,255,0.30),
      0px  1px 1px 0px rgba(0,0,0,0.20);
  }

  /* Icon nav items */
  .sidebar-item-icon {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .nav-icon {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
    opacity: 0.45;
    color: #222;
    transition: opacity 0.15s;
  }

  .sidebar-item-icon:hover .nav-icon { opacity: 0.7; }
  .sidebar-item-icon.active .nav-icon { opacity: 0.75; }

  .nav-label { flex: 1; min-width: 0; }

  /* Badge */
  .badge-hover {
    visibility: hidden;
    opacity: 0;
    transition: opacity 0.15s;
  }
  .sidebar-item-icon:hover .badge-hover,
  .sidebar-item-icon.active .badge-hover {
    visibility: visible;
    opacity: 1;
  }

  .sidebar-badge {
    background: #ececec;
    color: #5c5c5c;
    font-size: 10px;
    letter-spacing: -0.2px;
    padding: 2px 4px;
    border-radius: 5px;
    line-height: 1.4;
    flex-shrink: 0;
  }

  /* ── Spacer ── */
  .sidebar-spacer { flex: 1; min-height: 32px; }

  /* ── Footer ── */
  .sidebar-footer {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding-top: 20px;
    border-top: 1px solid #f0f0f0;
    flex-shrink: 0;
  }

  /* ── Partner ── */
  .sidebar-partner {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .sidebar-partner-label {
    font-size: 12px;
    color: #a4a4a4;
    line-height: 1.191;
    margin: 0;
    padding: 0 8px;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .partner-slider {
    position: relative;
    width: 100%;
    border-radius: 8px;
    overflow: hidden;
  }

  .partner-track {
    display: flex;
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .partner-slide {
    flex: 0 0 100%;
    width: 100%;
    display: block;
    position: relative;
  }

  .partner-slide img { width: 100%; height: auto; display: block; }

  .slide-tooltip {
    position: absolute;
    bottom: 8px;
    left: 8px;
    background: rgba(0, 0, 0, 0.55);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    color: #fff;
    font-size: 10px;
    padding: 3px 8px;
    border-radius: 5px;
    white-space: nowrap;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s ease;
    letter-spacing: 0.1px;
  }
  .partner-slide:hover .slide-tooltip { opacity: 1; }

  .slider-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: rgba(255,255,255,0.9);
    backdrop-filter: blur(4px);
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #111;
    opacity: 0;
    transition: opacity 0.2s, transform 0.2s;
    z-index: 5;
    padding: 0;
  }

  .slider-prev { left: 6px; }
  .slider-next { right: 6px; }
  .partner-slider:hover .slider-btn { opacity: 1; }
  .slider-btn:hover { transform: translateY(-50%) scale(1.1); background: #fff; }

  .slider-dots {
    position: absolute;
    bottom: 6px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 4px;
    z-index: 5;
  }

  .slider-dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: rgba(255,255,255,0.5);
    border: none;
    cursor: pointer;
    padding: 0;
    transition: background 0.2s, transform 0.2s;
  }
  .slider-dot.active { background: #fff; transform: scale(1.3); }

  /* ── Copyright ── */
  .sidebar-copyright { display: flex; flex-direction: column; gap: 4px; }

  .sidebar-copyright-divider { width: 100%; height: 1px; background: #f0f0f0; }

  .sidebar-copyright-text {
    font-size: 10px;
    color: #7a7a7a;
    line-height: 1.191;
    white-space: nowrap;
  }

  /* ── Mobile ── */
  @media (max-width: 768px) {
    .sidebar-close {
      display: flex;
      align-items: center;
      justify-content: center;
      position: absolute;
      top: 16px;
      right: 16px;
      background: none;
      border: none;
      cursor: pointer;
      color: #111;
      padding: 4px;
      z-index: 10;
    }

    .sidebar {
      width: 100%;
      transform: none;
      display: none;
    }

    .sidebar.open {
      display: block;
      box-shadow: none;
    }

    .sidebar-inner {
      position: relative;
      height: 100%;
      overflow-y: auto;
    }

    .sidebar-logo { margin-top: 8px; }
  }
</style>

<style is:global>
/* ── Search modal ── */
.sm-overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 500;
  align-items: flex-start;
  justify-content: center;
  padding-top: 80px;
}

.sm-overlay.open {
  display: flex;
}

.sm-modal {
  width: 560px;
  max-width: calc(100vw - 32px);
  background: #fff;
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 24px 80px rgba(0, 0, 0, 0.2), 0 4px 16px rgba(0, 0, 0, 0.08);
  display: flex;
  flex-direction: column;
  max-height: calc(100vh - 160px);
}

.sm-input-row {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 14px 18px;
  flex-shrink: 0;
}

.sm-search-icon {
  width: 18px;
  height: 18px;
  opacity: 0.35;
  flex-shrink: 0;
}

#sm-input {
  flex: 1;
  border: none;
  outline: none;
  font-size: 16px;
  font-family: 'Aileron', sans-serif;
  color: #111;
  background: transparent;
  min-width: 0;
}

#sm-input::placeholder { color: #bbb; }

.sm-esc-key {
  font-size: 10px;
  color: #bbb;
  background: #f2f2f2;
  border: none;
  border-radius: 4px;
  padding: 3px 7px;
  font-family: 'Aileron', sans-serif;
  flex-shrink: 0;
  cursor: default;
}

.sm-divider { height: 1px; background: #f0f0f0; flex-shrink: 0; }

.sm-results {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  overscroll-behavior: contain;
  padding: 8px;
  display: flex;
  flex-direction: column;
  scrollbar-width: thin;
  scrollbar-color: #e0e0e0 transparent;
}

.sm-hint {
  font-size: 13px;
  color: #bbb;
  margin: 8px 0;
  padding: 8px;
  text-align: center;
}

/* Result items */
.sm-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 10px;
  border-radius: 8px;
  text-decoration: none;
  transition: background 0.1s;
  flex-shrink: 0;
}

.sm-item:hover { background: #f5f5f5; }

.sm-item-avatar {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  object-fit: cover;
  flex-shrink: 0;
  border: 1px solid #eee;
}

.sm-item-initials {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  background: #2f2f2f;
  color: #fff;
  font-size: 11px;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.sm-item-title {
  flex: 1;
  font-size: 14px;
  color: #111;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
  min-width: 0;
}

.sm-item-cat {
  font-size: 12px;
  color: #aaa;
  flex-shrink: 0;
  white-space: nowrap;
}

@media (max-width: 600px) {
  .sm-overlay { padding-top: 0; align-items: flex-end; }
  .sm-modal {
    width: 100%;
    max-width: 100%;
    border-radius: 20px 20px 0 0;
    max-height: 80vh;
  }
}
</style>
